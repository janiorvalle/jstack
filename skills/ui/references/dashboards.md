# Dashboards

Rules for dashboard layouts, stat grids, KPI cards, metric cards, admin panels, analytics views, and summary data.

## Design rules

- Stat and metric card titles never wrap. Use `truncate`
- No icons in stat or metric cards. Plain text labels and values only

## Coding rules

- Responsive dashboard widgets always use container queries, not media queries
