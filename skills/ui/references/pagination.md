# Pagination

These rules apply to page number links, previous and next buttons, and any other paged navigation control.

- If the pagination has both page numbers and previous/next buttons, hide the page numbers on mobile.
