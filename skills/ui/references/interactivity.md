# Interactivity

Rules for hover states, transitions, animations, active states, and how clickable and non-clickable elements behave.

- Never add `hover:*` states to non-interactive elements. Save them for buttons, links, and other clickable elements
- Never add `transition-*` for hover color or background changes. Save transitions for elements that move or transform
