# Feature lists

Use these rules for feature grids, benefit lists, product feature sections, and any section that lists several features with titles and descriptions.

- Feature sections that list several features use `<dl>`, `<dt>`, and `<dd>` elements, not `<ul>`/`<li>` or plain `<div>` groups
