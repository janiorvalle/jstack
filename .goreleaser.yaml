version: 2

project_name: jstack

builds:
  - id: jstack
    main: ./cmd/jstack
    binary: jstack
    env:
      - CGO_ENABLED=0
    flags:
      - -trimpath
    ldflags:
      - -s -w
      - -X main.version={{ .Version }}
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64

archives:
  - id: jstack
    formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  groups:
    - title: Features
      regexp: '^.*feat(\([^)]*\))?:'
      order: 0
    - title: Fixes
      regexp: '^.*fix(\([^)]*\))?:'
      order: 1
    - title: Refactoring
      regexp: '^.*refactor(\([^)]*\))?:'
      order: 2
    - title: CI
      regexp: '^.*ci(\([^)]*\))?:'
      order: 3
    - title: Documentation
      regexp: '^.*docs(\([^)]*\))?:'
      order: 4
    - title: Other
      order: 999
  filters:
    exclude:
      - "^test:"
